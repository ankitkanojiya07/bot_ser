## Learned User Preferences
- Wants run settings (promoter IDs, count, min/max per minute, language, and form URL) changeable from the web UI, not hardcoded.

## Learned Workspace Facts
- Form URL is configurable from the UI; the default is https://seeedemaseekhelp.com/Weekend_Activity_4/ (not the older README `kanwar_yatra` path).
- The web UI is served by `server.js`; `fill-form.js` performs Playwright submissions.
